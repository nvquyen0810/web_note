# Local Keycloak realm

`realm-webnote.json` is imported when the Keycloak container first starts.

For local development only, sign in with username `demo` and password `demo`.
Replace all demo credentials outside local development.
